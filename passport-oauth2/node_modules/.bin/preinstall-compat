#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../make-node/bin/preinstall-compat.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../make-node/bin/preinstall-compat.sh" "$@"
  ret=$?
fi
exit $ret
