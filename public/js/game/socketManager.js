/* Can probably just remove this, seems pointless right now */
$(function(){
	var socketId = Global.QueryStringValue("id");
	var userName = Global.QueryStringValue("name")
	var user = undefined;

	var serverTimeOffset;
});